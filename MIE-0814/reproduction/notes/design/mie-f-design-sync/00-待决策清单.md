# 分文档 00：待决策清单（唯一 human 入口）

> [⬆ 返回主文档](PROJECT-SYNC-2026-08-11.md)

> **这是你唯一需要拍板的地方**。每项待决策含：**前因后果**（背景/轮次/证据）+ **待决策内容**（选项）+ **推荐选项**（理由）。
> 决策后：主文档状态表 + 相关副文档同步更新（按 doc-sync 同步规范）。

---

## 决策 1：Grahn gate④ 是否关闭 — 待决策

### 前因后果

**为什么需要决定**：Grahn 第 3 轮（2012 NJP 映射验证）数值验收全部通过，但 gate④（最终人工裁决）停在人 gate——B14 审查明确"材料完整，人工 gate 未关闭"。这是 4 轮中唯一有完整裁决材料待你拍板的 gate。

**已经走过的轮次**：
| 轮次 | 结果 |
|------|------|
| 09-12 形式化对抗审查 | 5 轮 BLOCKED → 修订至 v4 |
| D1 #14 → #14b | BLOCKED → PASS |
| A1 独立复核 | 逐位一致 |
| B6 报告 | overall_gate=PASS_WITH_NOTES / result_class=method_consistency |
| B18 材料 | 建议 PASS_WITH_NOTES (method consistency only) |

**核心矛盾/证据**：机器层全部 PASS（Path A 5.7e-7 / Path B 2.94e-4 / 逐 m 1.52e-3 / Rayleigh 6.098 / 光学定理 5.96e-7 / 解析 fixture 1.19e-14 / miepython 1.59e-15 / 远场 7.74e-15），但**无实验/论文图直接对比** → ceiling 只能 method_consistency，不能 physical_reproduction_success。

**外部基准新发现**（2026-08-11）：子 agent 调研找到 **arXiv:2508.16545**（Grahn 课题组 2025：Kołkowski/Sehrawat/Shevchenko，用 Grahn 方法 + 与 Mie theory overlap perfectly + 丰富数值 Fig.2/3）。B21（进行中）把我们的实现与该论文对比——**若一致，ceiling 可能从 method_consistency 提升**。

**为什么原来无图可对比**（2026-08-11 核实）：Grahn 2012 是**纯理论论文**，全文只有 2 张**原理示意图**（图 1 点电流元示意 / 图 2 点八极-偶极场等价原理图），**无数值结果曲线、无实验数据、无验证 section**（对比 Alaee 2018 有 Fig.1-3 数值曲线可提取）。因此第 3 轮只能与 Mie 解析真值交叉验证（Path A/B/逐 m/光学定理等），证明"映射方法实现与解析真值一致"= method_consistency；无法证明"匹配论文某个数值结果"（论文没有这样的结果）。证据：`C:/Users/27370/Desktop/project/zotero/papers/mie-f/grahn2012-chinese.tex`（图 1 在 L295、图 2 在 L447，均含 caption 标注示意性质）。
证据：`C:/Users/27370/Desktop/project/zotero/papers/mie-f/reproduction/codex-prompts/out/B18-grahn-gate4-material.md`

#### 待决策内容

| 选项 | 说明 | 代价 |
|------|------|------|
| **A. 批准关闭** | 接受 PASS_WITH_NOTES (method consistency) + 3 notes（mask 口径/EQ rank-4 截断/ceiling） | 关闭后 Grahn 轮完结；不宣称更多 |
| **B. 拒绝/暂缓** | 认为还需补充（如实验对比/论文图量化） | Grahn 轮保持未关，材料已在 |

#### 推荐选项

**推荐 A（批准关闭）**，理由：
1. 数值验收全 PASS + 机器层 overall_gate=PASS_WITH_NOTES
2. ceiling=method_consistency 已诚实（不冒充物理复现）
3. 3 notes 是口径说明非缺陷（mask 是 spec 预注册、EQ 是 rank-4 诊断、ceiling 是固有上限）

> 若选 B：需明确指出还差什么（如"要 Grahn 论文图对比"），我将评估可行性。

---

## 决策 2：Fig.3 COMSOL 真解推进方式 — 待决策

### 前因后果

**为什么需要决定**：Fig.3 是 4 轮中唯一 NOT_ACHIEVED（B7=surrogate_fallback、B8=diagnostic_only）。Java builder 已写出（B17，双金盘 + JC 色散 + SBC/PML + 场导出 + 多极矩节点，310 行静态过），但**本地无 comsol.jar 工具链**（javac/comsol 不在 PATH），Magnus batch_mph 阻塞于 blueprint FileSecret 配置（管理员）。

**已经走过的轮次**：
| 轮次 | 结果 |
|------|------|
| A3 探索 | GUI 模板是唯一硬前置 |
| B8 骨架 | Java 骨架 + runtime smoke 过 |
| B9s 文档 | 官方 MPH 路径 + Java 草案 + 规避设置 |
| B16 | 官方 6.3 rib MPH 本地验证（10 neff + 场型合理）；Magnus 阻塞 |
| B17 | Java builder 完整写出；本地编译 BLOCKED（缺 jar） |

**核心矛盾/证据**：物理设置已全部明确（B17 builder 即证据），只差**编译/求解环境**。这不是"缺知识"而是"缺工具"。
证据：`C:/Users/27370/Desktop/project/optics_agent/comsol/runtime/cases/alaee2018_fig3/Alaee2018Fig3ComsolScattering.java`

#### 待决策内容

| 选项 | 说明 | 代价 |
|------|------|------|
| **A. 本地装 COMSOL Java API** | 装 COMSOL 6.3（含 Java API jar），本地编译+跑 | 需 license/安装（可能贵/重） |
| **B. 等管理员更新 Magnus blueprint** | 让管理员把 case_bundle_secret 注册为 FileSecret，batch_mph/Java 可跑 | 需管理员动作；时间不确定 |
| **C. 搁置 Fig.3 COMSOL** | 维持 surrogate_fallback + NOT_ACHIEVED 诚实状态 | Fig.3 保持未完成；不耽误其他轮 |
| **D. GitHub 挂载（新，推荐）** | 用户推送专用文件夹（Java/MPH 输入）到 GitHub 仓库 → Magnus job 提交时指定 repo_name/branch/namespace，Preparing 阶段 ensure_repo() 自动 clone 进 job 工作目录挂载 | 需用户推送一次；仓库需公开或 Magnus 服务器 SSH 密钥可访问 |

**D 的机制（2026-08-11 源码确认）**：
- Job model 有 `namespace/repo_name/branch/commit_sha` 字段（`magnus-main/back_end/server/models/_job.py:56-59`）
- Preparing 阶段 `_resources.py:143` 调 `ensure_repo()` → `_repos.py` clone GitHub → `job_working_table/repository/` → wrapper 挂载进容器 `$MAGNUS_HOME/workspace/repository`
- HPC（Magnus）用 SSH `git@github.com:...`（服务器有部署密钥）；local 用 HTTPS
- **这绕开 B16/B17 的 FileSecret 阻塞**（不用上传 MPH/Java，推 GitHub 即被挂载）
- ⚠️ 子 agent 提醒：注意输出文件实际被临时目录删的情况（挂载目录临时性）

#### 推荐选项

**推荐 D（SSH 直放挂载路径，已落地）**，理由：
1. **B20 已实证**：探针 PASS（job 读到 /data/public 文件）+ batch_java 编译 PASS——挂载路径完全打通
2. 三家调研（IND1/2/3）独立确认：平台上 32 个成功 COMSOL job 全走挂载路径，FileSecret 在 comsol 镜像不可用（SDK 冲突）
3. **B22 进行中**：save→launch 显式声明 ephemeral_storage（SLURM 需显式）+ 输出指挂载盘，解决 B20 的 ENOSPC

> 若选 C：Fig.3 以 surrogate_fallback + NOT_ACHIEVED 收尾（诚实），文档已完整记录路径与阻塞。
> 若选 B：等管理员 FileSecret 配置，时间不确定（且 comsol 镜像 SDK 冲突需排查）。

**最新进展（2026-08-12）**：
- libpskernel.so 已找到（agent 从 COMSOL 6.2 官方 ISO 提取，87MB Parasolid 内核，SHA 271a36bd）→ 已 scp 到 `/data/public/zhangyuanzheng/mie-f-fig3/libs/`（SHA 确认）
- **B24 跑中**：挂载该库 + LD_LIBRARY_PATH 重跑 Java 3-D——若成功 → ED/MD/EQ/MQ 到手 → Fig.3 G4 突破
- 风险预判：6.2 库 vs 6.3 libcsjcad 符号兼容性（若失败需 6.3 版库）

**D 的机制（2026-08-11 源码确认 + B20 实证）**：
- Job model 有 namespace/repo_name/branch/commit_sha（github 挂载）；但**实际用的 SSH 直放**：文件放 /data/public/zhangyuanzheng/mie-f-fig3/ → job 平级权限读挂载盘 → batch_mph/Java
- 提交方式三种：空白提交（❌ 忽略资源参数）/ 提交 blueprint（定义）/ **通过 blueprint 提交 job（✅ save→launch 显式资源）**
- 参考已验证代码：PHY-LLM-Basic-Algorithm/train_zyz/submit_sft.py（save→wait 2s→launch→monitor）
- 资源显式声明：gpu_count/gpu_type/cpu_count/memory_demand/ephemeral_storage/priority

---

## 决策 3：Fig.2 B3 UQ 处理 — 待决策

### 前因后果

**为什么需要决定**：B3 按 A5-v2 预注册 spec 跑完 Fig.2 的 UQ，8 条正式 lane（panel a/b × ED/MD/EQ/MQ）全部 UNRESOLVED、promotion DENIED——这是 fail-closed 的诚实结果（calibrated 不确定性/有效 coverage/dense-mode 专属门槛不满足）。当前 result_class=partial_physical_match。

**已经走过的轮次**：
| 轮次 | 结果 |
|------|------|
| A5 预注册 | MQ 峰区 90.67% 贡献 → UNRESOLVED |
| B3 执行 | 8 lane 全 UNRESOLVED + DENIED（诚实） |
| B14 审查 | 确认披露完整、无 class overclaim |

**核心矛盾/证据**：UQ 需要更多数据/方法（calibrated 不确定性、有效 coverage）才能晋级，当前证据不足是**事实**非缺陷。
证据：`C:/Users/27370/Desktop/project/zotero/papers/mie-f/reproduction/codex-prompts/out/B3-promotion-verdict.md`

#### 待决策内容

| 选项 | 说明 | 代价 |
|------|------|------|
| **A. 维持现状** | 保持 partial_physical_match + promotion DENIED | Fig.2 诚实收尾；UQ 留待未来 |
| **B. 补 UQ 数据** | 增加 calibrated 不确定性/有效 coverage 计算 | 需更多计算时间 + 方法工作 |
| **C. 降级** | 因 UQ 不足降级 result_class | 低估了方法层已 PASS 的事实 |

#### 推荐选项

**推荐 A（维持现状）**，理由：
1. B3 的 UNRESOLVED 是预注册 fail-closed 的正确执行（非失败）
2. 方法层（表2 vs Mie <1%）已 PASS，partial_physical_match 是平衡的 class
3. 降级/升级都扭曲了事实

> 若选 B：我将规划 UQ 数据补充任务（calibrated 不确定性 + 有效 coverage）。

---

## 决策 4：Fig.3 正式晋级 or PARTIAL_PASS 收尾 — 待决策

### 前因后果

**为什么需要决定**：B29-B31 已拿到 **9 点 COMSOL 真解谱**（ED 主导 + MD/EQ x=0.36 磁共振 + vs 论文 ED r=0.999）。但 B31 诚实裁决 `PARTIAL_PASS`：缺正式网格收敛（0.7/0.5）、4000nm 外域、远场/功率闭合、vector paper trace、human gates。是否补这些晋级材料，还是以 PARTIAL_PASS 收尾？

**已经走过的轮次**：B20-B28 逐层突破（挂载/库/mesh/材料/后处理/背景场）→ B29 单点 → B30 9 点扫描 → B31 对比+裁决

**核心矛盾/证据**：谱物理正确（磁共振 + 论文形态一致），但正式晋升需 5 项额外证据（远端空间限制 + vector trace 需 human 批准 raster 替代）。
证据：`C:/Users/27370/Desktop/project/zotero/papers/mie-f/reproduction/codex-prompts/out/B31-report.md`

#### 待决策内容

| 选项 | 说明 | 代价 |
|------|------|------|
| **A. 补晋级材料** | 释放远端空间 → 0.7/0.5 网格 + 4000nm 外域 + 远场闭合 + vector trace | 需 1-2 天（CODEX + 你的 raster 替代批准） |
| **B. PARTIAL_PASS 收尾** | Fig.3 以 diagnostic_only + 9 点谱成果收尾 | 诚实收尾；未来可补 |
| **C. 仅补关键项** | 只补远场闭合 + 高阶 multipole 收敛（最关键的 2 项） | 半天；其余留未来 |

#### 推荐选项

**推荐 C（仅补关键项）**，理由：
1. 远场/功率闭合是最核心的科学验证（谱已对论文一致，闭合补最后一块）
2. 网格收敛已有单侧证据（coarse→baseline ≤1%），0.7/0.5 可后续补
3. vector trace 需你批准 raster 替代（决策成本低但需你拍板）

> 若选 B：Fig.3 以 9 点谱 + PARTIAL_PASS 诚实收尾（成果已显著）。

---

## 已决策（历史，供参考）

| 日期 | 决策 | 结果 |
|------|------|------|
| 2026-08-09 | Fig.1 gate④ 历史关闭 | PASS_WITH_LIMITATIONS（旧口径） |
| 2026-08-11 | **Fig.1 指标口径裁决** | 复矩口径（B13 审查 + superseding receipt），ED/MD >100% → PASS |
| 2026-08-11 | **B14 全栈审查 5🔴+8🟡** | 全部闭环（B15 修复 + 主 agent 补 receipt） |
| 2026-08-11 | **Fig.3 spec 晋升** | B11 修订稿 + B12 审查 → APPROVED_AS_PATH_CANDIDATE |
| 2026-08-12 | **Grahn gate④ 关闭** | 用户接受 PASS_WITH_NOTES（收据 GRAHN-G4-20260812） |
| 2026-08-12 | **Fig.3 晋级 + Fig.2 补 UQ** | 用户拍板（B32/B33 推进中） |

> ADR 原则：记录*已决定*和*为什么*，单段可记录；难回退 / 无上下文会奇怪 / 真实权衡的决策才记录。
