# Bohren & Huffman 1983 escalation list for pages 141-160

- Resolved: book page `142` was rechecked in `papers/mie-f/_pdf_review/reports/continued/Bohren_Huffman_1983_p142-142_continued.md`; the dense Rayleigh-scattering matrix page now has a page-level PASS.
- Recheck book page `148` because the OCR baseline reads `ELLIPSOD` in the section heading and `G(\eta,\xi)` where the render shows `ELLIPSOID` and `G(\eta,\zeta)`.
- Recheck book page `152` because OCR captured only the page header and diagram/labels.
- Recheck book page `154` because only the header was confirmed.
- Recheck book page `156` because only the header was confirmed.
- Recheck book pages `158-160` because OCR degraded to noise or timeout on the final pages of the chunk.
